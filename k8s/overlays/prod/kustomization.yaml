apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: money-talks-prod

# secrets.yaml excluded from Git (.gitignore) — managed manually.
# Will be replaced by SealedSecrets in Phase 4.
resources:
- ../../base
- namespace.yaml
- ingress.yaml
- secrets.yaml

images:
- name: BACKEND_IMAGE
  newName: REGION-docker.pkg.dev/YOUR_PROJECT_ID/YOUR_REGISTRY/money-talks-backend
  newTag: latest
- name: FRONTEND_IMAGE
  newName: REGION-docker.pkg.dev/YOUR_PROJECT_ID/YOUR_REGISTRY/money-talks-frontend
  newTag: latest
  
replicas:
- count: 3
  name: backend
- count: 2
  name: frontend
